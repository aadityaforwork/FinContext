# Pinned below 2.0 — the SDK renamed FastMCP -> MCPServer in v2 (import path
# changes from mcp.server.fastmcp to mcp.server.mcpserver). This server uses
# the v1 FastMCP import; bump the pin and update server.py's import together
# if you deliberately move to v2.
mcp[cli]>=1.2.0,<2.0.0
httpx>=0.27.0
